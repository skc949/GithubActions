name: call-reusable-from-another-repo

on:
  workflow_dispatch:
  push:
    branches:
      - dev
jobs:
  ci-pipeline-job:
    uses: skc949/reusablewf/.github/workflows/blank.yml@main
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}